name: Hello World
on: [push]
jobs:
        HelloWorld:
                runs-on: ubuntu-latest
                steps:
                        - run: echo '#include <iostream> int main(){ std::cout << "Hi";}' > hello.cpp
                        - run: ./hello
                
